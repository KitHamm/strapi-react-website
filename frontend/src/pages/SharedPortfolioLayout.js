import { Outlet } from "react-router-dom";

const SharedPortfolioLayout = () => {
    return (
        <div className="">
            <Outlet />
        </div>
    );
};
export default SharedPortfolioLayout;
